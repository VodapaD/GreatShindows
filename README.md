<h2>AutoHotKey Script</h2>

В данном репозитории хранится скрипт для автоматизации интерфейса Windows и расширения возможностей мыши.

## Структура
* **Скрипт:** `AutoHotkey/combination.ahk`
* **Версия AHK:** v2.0+

---

## ⌨️ Горячие клавиши (Hotkeys)

| Сочетание | Действие | Описание |
| :--- | :--- | :--- |
| **Win + T** | Терминал | Если Windows Terminal запущен — активирует его окно. Если нет — запускает новый процесс `wt.exe`. |
| **Win + B** | Браузер | Ищет открытые окна браузеров (Chrome, Edge, Firefox, Opera, Yandex, Brave, Zen). Если браузер найден — переключает на него, если нет — открывает домашнюю страницу по умолчанию. |
| **Win + C** | Закрыть окно | Мгновенно закрывает текущее активное окно. |
| **Win + S** | Центрировать | Плавно перемещает текущее окно в центр экрана с эффектом торможения (Ease-Out) в течение 0.3 сек. |
| **Win + PgUp** | Развернуть | Разворачивает активное окно на весь экран. |
| **Win + PgDn** | Свернуть | Сворачивает активное окно. |

---

## 🖱️ Функции мыши

### Скроллинг зажатым колесиком (MButton)
Позволяет прокручивать страницы, удерживая среднюю кнопку мыши:
* **Механика:** При зажатом колесике движение мыши вверх/вниз имитирует вращение `WheelUp` / `WheelDown`.
* **Стабилизация:** Курсор фиксируется на месте, а его физическое перемещение блокируется, чтобы фокус не уходил с нужной области.
* **Скорость:** Количество шагов прокрутки зависит от амплитуды движения мыши.

### Активный угол (Hot Corner)
Эмуляция функции "Обзор" (Task View) при наведении курсора:
* **Триггер:** Наведение мыши в **верхний левый угол** экрана (зона 3x3 пикселя).
* **Задержка:** Срабатывает через 50 мс, чтобы избежать случайных активаций.
* **Умный режим:** Функция автоматически отключается, если открыто полноэкранное приложение (игра или видео), а также если активен рабочий стол.

---

## 🛠️ Установка
1. Установите [AutoHotkey v2](https://www.autohotkey.com/).
2. Скачайте файл `combination.ahk`.
3. Запустите скрипт (двойной клик по файлу).

---

## 💡 Требования
* Для работы **Win + T** в системе должен быть установлен `Windows Terminal`.
* Для работы **Win + B** скрипт настроен на поиск популярных браузеров по именам их исполняемых файлов (`chrome.exe`, `msedge.exe` и др.).
